[package]
name = "arachne-naive-interpreter"
version = "0.1.0"
edition = "2021"

[dependencies]
async-stream = "0.3.5"
futures = "0.3.28"
runtime = { package = "arachne-runtime", path = "../../runtime" }

[dev-dependencies]
pretty_assertions = "1.3.0"
